# 时间管理小精灵（TimeManagementElf）Web 端开发文档 v0.2

## 一、项目背景与目标
- 专注于 Web 端开发，提供完整的时间管理功能：任务列表管理、番茄钟计时、数据记录与统计。
- 核心功能：任务列表（增删改查、排序过滤）、番茄钟（5/10/25/30 分钟与任务类型）、数据记录与统计。
- 任务数据以服务器为唯一真源（Source of Truth）：所有任务通过后端数据库同步；每个用户拥有独立任务集合（逻辑隔离，建议物理上通过 `user_id` 字段与索引实现）。

## 二、技术选型（Web 端）
### 主方案：React + TypeScript
- 框架：React 18 + TypeScript + Vite
- 状态管理：Zustand 或 Redux Toolkit
- 样式：Tailwind CSS + CSS Modules
- 路由：React Router v6
  - 存储方案：
    - IndexedDB（idb 库）用于本地数据缓存和离线支持
    - LocalStorage（用户设置）
    - 服务器数据库（主要数据存储）

## 三、Web 端架构设计
### 1) 数据存储与同步
- 本地存储：IndexedDB 保存任务和番茄钟记录（离线缓存）
- 数据同步：与后端 API 双向同步，支持离线操作
- 数据持久化：所有数据最终保存到服务器数据库
- 离线支持：网络断开时本地操作，恢复后自动同步

### 2) 状态管理与事件流
- 状态管理：Zustand 或 Redux Toolkit
- 事件流：
  - 任务选择 → 启动番茄钟
  - 番茄钟结束 → 记录写入数据库 → 数据同步
  - 数据同步 → UI 更新 → 本地缓存更新

## 四、模块设计（含任务列表）
### 1) 任务列表模块
#### 功能
- 列表展示：任务名称、类型、优先级、预计时长、标签、状态（待办/进行中/已完成）。
- 选择任务启动番茄钟：任务行上的“开始计时”按钮或进入详情后启动；默认时长可来自任务配置或通用设置。
- 增删改查：新增任务、编辑字段、标记完成、软删除/归档。
- 排序与过滤：按优先级、类型、标签、创建时间排序与筛选。
 - 预计与实际对比：任务的 `estimate_minutes` 为预估长度；实际耗时由番茄会话 `actual_minutes` 累计得到，用于对比与生成统计报表。

#### 存储与同步策略
- 服务器为唯一真源：所有任务 CRUD 操作通过后端 API 完成；客户端维护本地缓存（DB/IndexedDB）以提升体验。
- 离线支持：操作采用 Outbox 模式入队；网络恢复后按顺序提交；冲突通过版本号或 `updated_at` 比较解决（默认 Last-Write-Wins，可选乐观锁）。
- 导出：客户端或服务器端支持导出 `tasks.csv`；推荐由服务器侧提供导出接口以支持大数据量与审计。

#### 与番茄钟联动
- 启动番茄钟时记录当前任务 `task_id`；结束后写入番茄记录并回写任务统计（累计专注分钟、完成次数等）。

### 2) 番茄钟模块（扩展）
- 时长：5/10/25/30 分钟；可自定义；与任务类型关联（映射到不同提醒或宠物反馈）。
- 操作：开始/暂停/恢复/重置；结束提醒（音效/系统通知/本地推送）。
- 计时精度：以实际时间差为准（`DateTime.now` 差值 + `Ticker` 修正）。
- 同步：番茄会话记录在本地生成后即推送服务器（包含 `user_id` 与 `task_id`）；失败则进入离线队列，重试直至成功。
 - 统计口径：每次会话结束写入 `actual_minutes`（实际耗时），并可附带 `planned_minutes`（本次计划时长）；任务侧统计实际总耗时与会话次数。




## 后端与同步架构（新增）
### 技术栈建议
- 后端：Node.js（NestJS/Express）或 Go（Gin/Fiber）。
- 数据库：PostgreSQL（推荐）或 MySQL。
- ORM：Prisma / TypeORM。
- 鉴权：JWT（Access + Refresh）或 OAuth2。
- 实时：WebSocket / SSE（任务更新推送、番茄完成广播）。

### API 设计（示例）
- Auth
  - POST `/api/auth/login` { email, password }
  - POST `/api/auth/refresh`
- Tasks
  - GET `/api/tasks`
  - POST `/api/tasks` { title, task_type, priority, tags, estimate_minutes }
  - PUT `/api/tasks/{task_id}` { ... , version }
  - PATCH `/api/tasks/{task_id}/complete` { summary }
  - DELETE `/api/tasks/{task_id}`
- Tomato Sessions
  - GET `/api/tomatoes`
  - POST `/api/tomatoes` { task_id, planned_minutes, started_at, ended_at, status, interruption_reason }
- Statistics
  - GET `/api/statistics/tasks`
  - GET `/api/statistics/tomato-sessions`
  - GET `/api/statistics/summary`

### 同步流程（客户端）
1. 登录后拉取增量：`GET /api/tasks?since=last_sync_at`。
2. 本地改动入 Outbox 队列；后台/前台定时批量提交。
3. 冲突处理：使用 `version` 或 `updated_at` 比较；默认 LWW，必要时提示用户选择合并方案。
4. 成功后更新本地缓存与 `last_sync_at`。
5. 统计更新：会话结束或任务完成后，服务器自动更新任务的累计实际耗时与会话次数；任务完成时提交 `summary`。

### 安全与隐私
- 所有请求需附带 Access Token。
- 审计：记录 `created_by`、`updated_by`、`ip`（可选）。

### 数据库表字段（Tasks）
- 表：`tasks`
  - `task_id` PK（UUID）、`user_id`（索引）
  - 业务字段：`title`, `task_type`, `priority`, `tags`, `estimate_minutes`
  - 状态字段：`status`, `completed_at`
  - 统计字段：`stats_focus_minutes`, `stats_actual_minutes`, `stats_sessions_count`
  - 总结字段：`summary`（任务完成总结，必填）
  - 同步/并发：`version`, `created_at`, `updated_at`

## 五、数据模型（数据库字段）
### 1) 任务列表（`tasks` 表）
- `task_id`（UUID，主键）
- `user_id`（UUID，外键，索引）
- `title`（字符串）
- `task_type`（字符串，如写作/阅读/编码）
- `priority`（`low`/`medium`/`high`）
- `tags`（JSON 数组）
- `estimate_minutes`（整数，可空）
- `created_at`（时间戳）
- `updated_at`（时间戳）
- `completed_at`（时间戳，可空）
- `status`（`todo`/`in_progress`/`done`/`archived`）
- `stats_focus_minutes`（累计专注分钟，整数）
- `version`（整数，自增，用于冲突检测）
- `stats_actual_minutes`（累计实际耗时，整数）
- `stats_sessions_count`（累计会话次数，整数）
- `summary`（任务完成总结，文本；当 `status=done` 时必填）

### 2) 番茄钟记录（`tomato_sessions` 表）
- `session_id`（UUID，主键）
- `user_id`（UUID，外键）
- `task_id`（UUID，外键，可空）
- `task_type`（字符串）
- `planned_minutes`（整数）
- `actual_minutes`（整数）
- `started_at`（时间戳）
- `ended_at`（时间戳）
- `status`（`completed`/`interrupted`/`cancelled`）
- `interruption_reason`（字符串，可空）

### 3) 任务统计与报表（数据库视图或查询）
- 计算字段：
  - `actual_total_minutes` = 所有关联会话 `actual_minutes` 之和
  - `sessions_count` = 关联会话数量
  - `avg_session_minutes` = `actual_total_minutes` / `sessions_count`
  - `variance_minutes` = `actual_total_minutes` - `estimate_minutes`
- 统计由服务器端计算，支持实时查询和缓存。


## 六、目录结构（React 项目）
```text
TimeManagementElf/
├─ public/
│  └─ sounds/
├─ src/
│  ├─ App.tsx
│  ├─ main.tsx
│  ├─ core/
│  │  ├─ store/           # 状态管理
│  │  ├─ services/
│  │  │  ├─ timerService.ts
│  │  │  ├─ storageService.ts  # IndexedDB 封装
│  │  │  ├─ apiService.ts      # 后端 API 调用
│  │  │  └─ syncService.ts     # 数据同步服务
│  │  ├─ types/
│  │  │  ├─ task.ts
│  │  │  └─ tomatoRecord.ts
│  │  └─ utils/
│  ├─ components/
│  │  ├─ Tasks/
│  │  │  ├─ TaskList.tsx
│  │  │  ├─ TaskEditor.tsx
│  │  │  └─ TaskItem.tsx
│  │  ├─ Tomato/
│  │  │  ├─ TomatoPanel.tsx
│  │  │  ├─ TimerDisplay.tsx
│  │  │  └─ ProgressRing.tsx     # 进度环动画
│  │  ├─ Statistics/
│  │  │  ├─ StatsPanel.tsx
│  │  │  └─ Charts.tsx
│  │  └─ common/
│  │     └─ TransitionWrapper.tsx # 页面切换动画
│  ├─ pages/
│  │  ├─ HomePage.tsx
│  │  ├─ TasksPage.tsx
│  │  └─ SettingsPage.tsx
│  ├─ styles/
│  │  ├─ animations.css          # CSS 动画定义
│  │  └─ components.css          # 组件样式
│  └─ assets/
│     └─ sounds/                 # 音效文件
├─ package.json
├─ vite.config.ts
└─ docs/
```

## 七、UI 与交互设计
### 主页面布局
- 顶部导航：Logo、任务列表、番茄钟、设置
- 左侧：任务列表区域（可折叠）
- 中央：番茄钟面板
- 右侧：统计面板（可选）

### 任务列表页
- 顶部：任务类型筛选、标签筛选、排序选择（优先级/创建时间）
- 列表项：标题、类型、优先级、预计时长、状态；操作按钮：开始计时、编辑、完成、更多
- 新建任务：弹窗或独立页，支持任务类型、时长、标签、优先级输入
- 完成任务：弹出"任务总结"编辑框，提交后调用 `PATCH /api/tasks/{task_id}/complete { summary }` 并更新统计

### 番茄钟面板
- 显示当前任务标题与类型；选择 5/10/25/30；开始/暂停/恢复/重置；结束提醒
- 进度环动画：使用 CSS `stroke-dasharray` 或 SVG 动画显示剩余时间
- 状态指示器动画：准备中/专注中/休息中的颜色和图标切换
- 按钮交互动画：悬停效果、点击反馈、状态切换过渡


## 八、计时精度与后台策略
- 计时实现：使用 `setInterval` + `Date.now()` 差值，避免 timer 漂移
- 页面非活跃处理：使用 Visibility API 检测页面状态，非活跃时暂停计时
- 时间校正：页面重新激活时计算时间差，校正计时器
- 提醒机制：使用 Web Notifications API 发送浏览器通知
- 数据持久化：计时状态保存到 IndexedDB，页面刷新后恢复

## 九、数据同步与离线支持
### 同步策略
- 实时同步：任务和番茄钟记录实时提交到服务器
- 离线支持：网络断开时数据保存在 IndexedDB，恢复后自动同步
- 冲突解决：使用版本号机制，支持 Last-Write-Wins 策略

### 实现方式
- 数据源：服务器数据库为主，IndexedDB 为本地缓存
- 同步队列：离线操作进入队列，网络恢复后批量提交
- 状态管理：同步状态实时反馈给用户界面
- 错误处理：网络错误时提供重试机制和用户提示

## 十、测试与验收
### 单元测试
- `TaskService`：增删改查、排序过滤、与 IndexedDB 同步
- `TimerService`：开始/暂停/恢复/结束精度
- `SyncService`：数据同步、冲突处理、离线队列
- 统计计算：实际总耗时、会话次数、偏差（与 `estimate_minutes` 的对比）

### 集成测试
- 任务 → 番茄钟完整流程
- 数据同步：本地 IndexedDB 与服务器 API 的双向同步
- 任务完成总结：当 `status=done` 时强制填写 `summary` 并正确同步到数据库
- 离线功能：网络断开时的数据保存和恢复同步
- 浏览器兼容性：Chrome、Firefox、Safari、Edge

### 验收标准
- Web 应用可在主流浏览器正常运行
- 核心功能完整：任务管理、番茄钟、宠物互动、数据导出
- 响应式设计：桌面端和移动端都有良好体验
- 性能要求：页面加载时间 < 3秒，动画流畅度 60fps

## 十一、开发里程碑
- M1（1周）：React 项目初始化、基础 UI 框架、路由配置、状态管理
- M2（1周）：任务列表模块、增删改查功能、排序过滤
- M3（1周）：番茄钟模块、计时器、提醒功能、与任务列表联动
- M4（1周）：数据存储（IndexedDB）、数据同步、设置面板
- M5（1周）：统计面板、数据可视化、性能优化
- M6（0.5周）：测试、浏览器兼容性、首版发布

## 十二、待确认项
- 前端框架确认：React + TypeScript（已选定）
- 状态管理选择：Zustand 或 Redux Toolkit？
- 样式方案：Tailwind CSS 或 CSS Modules？
- 后端技术栈：Node.js/NestJS 或 Go；数据库选型（PostgreSQL/MySQL）
- 是否需要实时推送（WebSocket/SSE）？
- 同步策略：是否接受 LWW + 版本号方案，或需要更严格的冲突提示与人工合并？
- 离线支持：离线操作的数据保留时长和同步策略？
- 统计功能：需要哪些数据可视化图表（饼图、柱状图、趋势图）？
- 音效支持：番茄钟提醒是否需要音效？
- 部署方案：Vercel、Netlify 还是自建服务器？